# PickVia v1.5.1

- Remove the DuckDuckGo private-mode version restriction. Private routing still requires a compatible unsandboxed build and keeps its isolated session data.
- Move the website language selector into the top bar beside Source, with matching rounded pill styling.
- Remove the homepage language notice and update all 80 website languages and 12 READMEs.

Requires macOS 14 or later on Apple Silicon. This download is signed with an Apple Development certificate and is not notarized. See the [installation instructions](https://github.com/kiteretsu903/pick-via#install).
